mutation RegistrationPageSignUp($input: TSignUpInput!) {
    auth {
        signUp(input: $input) {
            accessToken
            accessTokenTTL
            refreshToken
            refreshTokenTTL
        }
    }
}